# Shared MySQL host settings
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=

# Consolidated database names (2-DB model)
CORE_DB_NAME=healthcare_core_db
COMMERCE_DB_NAME=healthcare_commerce_db

# Optional per-service full URL override (only if needed)
# DB_URL=jdbc:mysql://localhost:3306/custom_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Pharmacy cross-read source DB (defaults to CORE_DB_NAME if unset)
# USER_DB_URL=jdbc:mysql://localhost:3306/healthcare_core_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
