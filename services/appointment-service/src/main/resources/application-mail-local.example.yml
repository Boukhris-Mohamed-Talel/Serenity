# Copy this file to application-mail-local.yml (exact name) in the same folder.
# Only application-mail-local.yml is loaded — editing THIS example file does nothing.
# application-mail-local.yml is gitignored — do not commit it.
#
# Google / Gmail SMTP (works for @gmail.com and many Google Workspace addresses like @esprit.tn):
#   - Enable 2-Step Verification on the Google account
#   - Create an "App password" and paste it below (spaces optional)
#
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: YOUR_EMAIL@example.com
    password: yywu nnso ihvv wcnd
